global:
  scrape_interval: 15s
  evaluation_interval: 15s
  scrape_timeout: 10s

# Prometheus scrape configuration for the local observability stack.
# - The OpenTelemetry Collector exposes a Prometheus-compatible metrics
#   endpoint (configured at 0.0.0.0:9464 in the collector config).
# - This file is intended to be mounted into the Prometheus container.
scrape_configs:
  # existing otelcol scrape job here ...

  - job_name: "deep-research-agent-backend"
    metrics_path: "/metrics"
    static_configs:
      #      - targets: ["localhost:8000"]
      - targets: ["172.29.192.1:8000"]
    honor_labels: true
    scrape_interval: 15s
    scrape_timeout: 10s
